@model IEnumerable<controlAdministrativo.Models.auditoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.accion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tabla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.modificadoPor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuarios.cedula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.padres.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.accion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tabla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.modificadoPor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuarios.cedula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.padres.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
